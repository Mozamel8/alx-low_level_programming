#include "main.h"

/**
 * _isupper - check if number upper
 * @c:character to be checked
 * Return: one if upper zero if not
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
