#include <stdio.h>

/**
 * main - prints all digit number base ten
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for(int i = 0; i< 10; i++)
	printf("%d", i);

	print ('\n');

	return (0);
}
